OUTPUT_FORMAT("elf64-x86-64")

ENTRY(_start)

PHDRS
{
    data    PT_LOAD;
    text    PT_LOAD;
    bss     PT_LOAD;
}

SECTIONS
{
    .data 0x100000 :
    {
        *(.data*)
        *(.got.plt*)
        *(COMMON*)
    } : data

    .text ALIGN(0x1000) :
    {
        *(.text*)
    } : text = 0x90909090

    .rodata :
    {
        *(.rodata*)
    } : text

    .bss ALIGN(0x1000) : {
        *(.bss*)
        PROVIDE(_bss_end = .);
    } : bss
}
