Import('env')
if env['PLATF'] == 'kachel':
    env = env.Clone()
    env["CXXFLAGS"] = env["CXXFLAGS"].replace('-flto','')
    env.M3Program(
        env,
        'standalone',
        [env.Glob('*.cc'), env.Glob('$ARCH/*.cc')],
        NoSup = True,
        libs = ["c"]
    )
