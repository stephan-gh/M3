[package]
name = "pager"
version = "0.1.0"

[lib]
path = "src/pager.rs"
crate-type = ["staticlib"]

[dependencies]
bitflags = "*"
m3 = { path = "../../libs/rust/m3" }
resmng = { path = "../../libs/rust/resmng" }
thread = { path = "../../libs/rust/thread" }
