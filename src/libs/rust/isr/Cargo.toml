[package]
name = "isr"
version = "0.1.0"
edition = "2018"

[lib]
path = "src/isr.rs"
crate-type = ["rlib", "staticlib"]

[dependencies]
base = { path = "../base" }
lang = { path = "../lang" }
paging = { path = "../paging" }
cfg-if = "1.0.0"
