import os

Import('env')

myenv = env.Clone()
myenv.Append(CXXFLAGS = ' -fno-rtti -fno-exceptions')

sources = [
    myenv.Glob('*.cc'), myenv.Glob('*/*.cc'),
    myenv.Glob('arch/$ARCH/*.S'), myenv.Glob('arch/$ARCH/*.cc'),
]

lib = myenv.StaticLibrary(
    target = 'libbase',
    source = sources
)
myenv.Install(myenv['LIBDIR'], lib)
