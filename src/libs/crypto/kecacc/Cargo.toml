[package]
name = "kecacc"
version = "0.1.0"
edition = "2021"

[dependencies]
base = { path = "../../../libs/rust/base", optional = true }

[features]
# backend-kecacc is the default, but can be overridden by specifying one of:
backend-xkcp = ["dep:base"]
